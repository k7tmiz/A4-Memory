// cloud.js stub — used when the private cloud module is not present
// The app handles missing A4Cloud gracefully (cloud buttons show errors without crashing)
console.warn("cloud.js not available — cloud features disabled")
